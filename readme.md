# CMake Template

```sh
mkdir build && cd build
cmake ..                         # configure
make                             # build
ctest                            # run tests
make install                     # install to CMAKE_INSTALL_PREFIX
```
